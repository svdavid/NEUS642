# Packages to Install for this Notebook 

Two new packages required. It's quickest to get them with pip:
```
pip install scikit-posthocs
pip install detecta
```

Alternatively, you can use conda to install these pacakges, but you need to specify the repository for detecta:
```
conda install scikit-posthocs
conda install -c duartexyz detecta
```
