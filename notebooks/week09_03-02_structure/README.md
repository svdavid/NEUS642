# NEUS 642 Week 9: PyMOL tutorial for visualization of protein structure (for non-structural biologists)

This week's setup should hopefully be pretty straightforward. 

* We'll just need to install PyMOL into our class environment:

`conda activate neus642   # or whatever your favorite environment is`

`conda install conda-forge::pymol-open-source`

  This can be kind of slow, but it does finish eventually!

* Then open today's notebook -- [Pymol_tutorial.iynb]

* If there are issues with images displaying, you might can try this alternative notebook -- [Pymol_tutorial_alt.iynb]. This version has an extra `ipython_image()` command at the end of each cell to generate a figure.